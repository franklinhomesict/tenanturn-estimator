import {useEffect,useMemo,useState} from 'react';

const logo='https://cdn.jobtread.com/G1QAaByn6Sr5CkWyvKV-3ezPG7pNFSoaG4EmdVj6QgQmcuAeoKXh5-_RKSYUv8M8_bELDZAQGdVpQRG0AMDYvmvJ2nwhUx4BbRjNGQ.9thaKOHAI7-2EsSPFpyFQIFL_VMd0XZq03jRYjrug5M?size=512';
const css=`*{box-sizing:border-box}body{margin:0;background:#0b0e13;color:#f3f6fa;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif}.app{max-width:1440px;margin:auto;padding:18px}.head{display:flex;justify-content:space-between;gap:18px;align-items:center}.brand{display:flex;gap:14px;align-items:center}.logo{width:72px;height:72px;object-fit:contain;border-radius:12px}.sub,.muted{color:#9ba7b7}.small{font-size:12px}h1{margin:0;font-size:clamp(25px,4vw,38px)}h2{margin:0;font-size:19px}h3{margin:0 0 10px;font-size:15px}.toolbar{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin:16px 0 13px}.toolbar button{border:1px solid #29313d;background:#10151d;color:#f3f6fa;padding:9px 12px;border-radius:10px;font-weight:750;cursor:pointer}.toolbar button.on{border-color:#6ab7ff;background:#152235}.toolbar input{border:1px solid #29313d;background:#10151d;color:#f3f6fa;padding:9px;border-radius:9px}.periodlabel{margin-left:auto;color:#9ba7b7;font-size:12px}.ownergrid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:12px 0 16px}.owner{appearance:none;text-align:left;color:inherit;background:#141922;border:1px solid #29313d;border-radius:16px;padding:16px;cursor:pointer;min-height:175px;transition:.15s transform,.15s border-color,.15s background}.owner:hover{transform:translateY(-1px);border-color:#475569}.owner.active{border-color:#6ab7ff;background:#152235;box-shadow:0 0 0 1px rgba(106,183,255,.14)}.ownerhead{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:9px}.arrow{color:#6ab7ff;font-size:20px}.hero{font-size:30px;font-weight:850;margin:6px 0}.line{display:flex;justify-content:space-between;gap:10px;border-top:1px solid #29313d;padding:6px 0;font-size:12.5px}.line span{color:#9ba7b7}.detail{background:#10151d;border:1px solid #29313d;border-radius:16px;padding:16px}.detailhead{display:flex;justify-content:space-between;align-items:flex-end;gap:12px;margin-bottom:12px}.grid{display:grid;grid-template-columns:repeat(12,minmax(0,1fr));gap:12px}.card{background:#141922;border:1px solid #29313d;border-radius:13px;padding:14px;min-width:0}.c12{grid-column:span 12}.c8{grid-column:span 8}.c6{grid-column:span 6}.c4{grid-column:span 4}.c3{grid-column:span 3}.metriclabel{font-size:10.5px;text-transform:uppercase;letter-spacing:.05em;color:#9ba7b7;font-weight:850}.metricvalue{font-size:27px;font-weight:850;margin-top:5px}.good{color:#52d28c}.warn{color:#ffd166}.bad{color:#ff6b6b}.blue{color:#6ab7ff}.notice{padding:10px 12px;border:1px solid #5b4b22;background:#18150d;border-radius:10px;color:#ffd166;font-size:12px;margin:10px 0}.notice.ok{border-color:#245b3f;background:#0e1814;color:#52d28c}table{width:100%;border-collapse:collapse;font-size:12.5px}th,td{padding:9px 8px;border-bottom:1px solid #29313d;text-align:left;vertical-align:top}th{color:#9ba7b7;font-size:10.5px;text-transform:uppercase}.empty{padding:22px;text-align:center;color:#9ba7b7}.foot{margin-top:10px;color:#778394;font-size:11px}@media(max-width:950px){.ownergrid{grid-template-columns:repeat(2,1fr)}.c8,.c6,.c4,.c3{grid-column:span 12}}@media(max-width:620px){.app{padding:12px}.head{align-items:flex-start}.logo{width:58px;height:58px}.ownergrid{grid-template-columns:1fr}.owner{min-height:0}.toolbar button{flex:1 1 28%}.periodlabel{width:100%;margin-left:0}.detailhead{align-items:flex-start;flex-direction:column}table,thead,tbody,tr,th,td{display:block;width:100%}thead{display:none}tr{border:1px solid #29313d;border-radius:10px;margin:0 0 9px;padding:8px;background:#10151d}td{border:0;padding:5px 4px}td:before{content:attr(data-label);display:block;color:#9ba7b7;font-size:10px;font-weight:800;text-transform:uppercase}}`;

const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(Number(n||0));
const pct=n=>`${Number.isFinite(n)?n.toFixed(1):'0.0'}%`;
const dateOnly=s=>s?new Date(`${String(s).slice(0,10)}T12:00:00`):null;
const ymd=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
const inRange=(s,a,b)=>{if(!s||!a||!b)return false;const d=dateOnly(s);return d>=dateOnly(a)&&d<=dateOnly(b)};
const sum=(a,f=x=>x)=>a.reduce((t,x)=>t+(Number(f(x))||0),0);
const perfItem=i=>!/reimburs|reimbursable|at cost/i.test(i?.name||'');
const startRx=/\b(started|starting|working|work today|progress|installed|installing|demo|painting|on site|onsite|crew|getting started)\b/i;
const doneRx=/\b(done|complete|completed|finished|ready to bill|wrapped up|wrap up|job is complete)\b/i;

function rangeFor(mode,customStart,customEnd){const n=new Date();let s=new Date(n);if(mode==='Week'){const day=(n.getDay()+6)%7;s.setDate(n.getDate()-day)}if(mode==='Month')s=new Date(n.getFullYear(),n.getMonth(),1);if(mode==='Quarter')s=new Date(n.getFullYear(),Math.floor(n.getMonth()/3)*3,1);if(mode==='Custom')return{start:customStart,end:customEnd};return{start:ymd(s),end:ymd(n)}}
function normalize(api){const p=api?.payload?.data||api?.payload||{};const o=p.organization||{};return{jobs:o.jobs?.nodes||[],docs:o.documents?.nodes||[],comments:o.comments?.nodes||[],logs:o.dailyLogs?.nodes||[],fetchedAt:api?.fetchedAt||null}}
function buildModel(data,start,end){
 const docs=data.docs||[],orders=docs.filter(d=>d.type==='customerOrder'),approved=orders.filter(d=>d.status==='approved'),pending=orders.filter(d=>d.status==='pending'),denied=orders.filter(d=>d.status==='denied'),invoices=docs.filter(d=>d.type==='customerInvoice'&&d.status!=='draft'),vendorOrders=docs.filter(d=>d.type==='vendorOrder'&&['approved','pending'].includes(d.status)),vendorBills=docs.filter(d=>d.type==='vendorBill'&&d.status!=='draft');
 const wins=approved.filter(d=>inRange(d.closedAt||d.issueDate,start,end)),approvedEver=new Set(approved.map(d=>d.job?.id)),losses=[...new Map(denied.filter(d=>!approvedEver.has(d.job?.id)&&inRange(d.closedAt||d.issueDate,start,end)).map(d=>[d.job?.id,d])).values()],winJobs=new Set(wins.map(d=>d.job?.id));
 const winRate=(winJobs.size+losses.length)?100*winJobs.size/(winJobs.size+losses.length):0;
 let revenue=0,cost=0,reimbursements=0;const periodInvoices=invoices.filter(d=>inRange(d.issueDate,start,end));for(const inv of periodInvoices){const items=inv.costItems?.nodes||[];if(items.length){for(const i of items){if(perfItem(i)){revenue+=Number(i.price||0);cost+=Number(i.cost||0)}else reimbursements+=Number(i.price||0)}}else{revenue+=Number(inv.priceWithTax||0);cost+=Number(inv.cost||0)}}
 const feeBills=vendorBills.filter(d=>inRange(d.issueDate,start,end)&&/payment processing fee|instant pay/i.test(`${d.account?.name||''} ${d.fullName||''}`)),fees=sum(feeBills,d=>d.cost),profit=revenue-cost-fees,margin=revenue?100*profit/revenue:0;
 const ar=invoices.filter(d=>Number(d.balance||0)>0),ap=vendorBills.filter(d=>Number(d.balance||0)>0);
 const by={};for(const j of data.jobs)by[j.id]={job:j,approved:[],vendors:[],invoices:[],comments:[],logs:[]};for(const d of approved)if(by[d.job?.id])by[d.job.id].approved.push(d);for(const d of vendorOrders)if(by[d.job?.id])by[d.job.id].vendors.push(d);for(const d of invoices)if(by[d.job?.id])by[d.job.id].invoices.push(d);for(const c of data.comments||[])if(by[c.job?.id])by[c.job.id].comments.push(c);for(const l of data.logs||[])if(by[l.job?.id])by[l.job.id].logs.push(l);
 const current=[];for(const x of Object.values(by)){if(x.job.closedOn||!x.approved.length)continue;const value=sum(x.approved,d=>d.priceWithTax),billed=sum(x.invoices,d=>d.priceWithTax),text=[...x.comments.map(c=>c.message),...x.logs.map(l=>l.notes)].join(' '),hasStart=x.logs.length>0||startRx.test(text),hasDone=doneRx.test(text);let stage='Backlog';if(x.vendors.length&&!hasStart)stage='Assigned / Not Started';if(x.vendors.length&&hasStart)stage='WIP';if(hasDone&&billed<value)stage='Ready to Bill';if(!x.vendors.length&&hasStart)stage='Review';if(hasDone&&billed>=value)stage='Billed / Open';current.push({id:x.job.id,job:x.job.name,value,billed,remaining:Math.max(0,value-billed),stage,vendor:x.vendors.map(v=>v.account?.name).filter(Boolean).join(', ')||'—'})}
 const backlog=current.filter(x=>x.stage==='Backlog'),assigned=current.filter(x=>x.stage==='Assigned / Not Started'),wip=current.filter(x=>x.stage==='WIP'),ready=current.filter(x=>x.stage==='Ready to Bill'),review=current.filter(x=>x.stage==='Review');
 const vendors={};for(const v of vendorOrders){const name=v.account?.name||'Unknown';vendors[name]??={name,assignments:0,active:0,value:0};vendors[name].assignments++;const c=current.find(x=>x.id===v.job?.id);if(c&&['Assigned / Not Started','WIP','Ready to Bill'].includes(c.stage)){vendors[name].active++;vendors[name].value+=c.remaining||c.value}}
 return{pending,wins,losses,winRate,salesWon:sum(wins,d=>d.priceWithTax),revenue,cost,reimbursements,fees,profit,margin,ar,ap,current,backlog,assigned,wip,ready,review,people:Object.values(vendors).sort((a,b)=>b.active-a.active||b.value-a.value),periodInvoices};
}
function Line({a,b,tone=''}){return <div className='line'><span>{a}</span><b className={tone}>{b}</b></div>}
function Metric({label,value,tone=''}){return <div className='card c3'><div className='metriclabel'>{label}</div><div className={`metricvalue ${tone}`}>{value}</div></div>}
function Table({headers,rows}){return <table><thead><tr>{headers.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((v,j)=><td key={j} data-label={headers[j]}>{v}</td>)}</tr>)}</tbody></table>}

export default function App(){
 const [active,setActive]=useState('Ops'),[mode,setMode]=useState('Month'),[customStart,setCustomStart]=useState('2026-09-01'),[customEnd,setCustomEnd]=useState('2026-09-06'),[api,setApi]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(true);
 const load=()=>{setLoading(true);setError('');fetch('/api/dashboard').then(async r=>({r,j:await r.json()})).then(({r,j})=>{if(!r.ok||!j.ok)throw new Error(j.error||'Live JobTread feed unavailable');setApi(j)}).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);
 const data=useMemo(()=>normalize(api),[api]),range=useMemo(()=>rangeFor(mode,customStart,customEnd),[mode,customStart,customEnd]),m=useMemo(()=>buildModel(data,range.start,range.end),[data,range.start,range.end]);
 const period=`${range.start} → ${range.end}`;
 const department={Sales:{hero:money(m.salesWon),lines:[['Pending bids',`${m.pending.length} · ${money(sum(m.pending,d=>d.priceWithTax))}`],['Won',`${new Set(m.wins.map(d=>d.job?.id)).size} jobs`],['Win rate',pct(m.winRate)]]},Ops:{hero:`${m.wip.length} WIP`,lines:[['Backlog',m.backlog.length],['Assigned',m.assigned.length],['Ready to bill',m.ready.length]]},People:{hero:`${m.people.filter(x=>x.active>0).length} active`,lines:[['Active assignments',sum(m.people,x=>x.active)],['Vendors in system',m.people.length],['Work represented',money(sum(m.people,x=>x.value))]]},Finance:{hero:money(m.profit),lines:[['Earned revenue',money(m.revenue)],['Margin',pct(m.margin)],['A/R',money(sum(m.ar,d=>d.balance))]]}};
 const Card=({name})=>{const d=department[name];return <button className={`owner ${active===name?'active':''}`} onClick={()=>setActive(name)} aria-pressed={active===name}><div className='ownerhead'><h2>{name}</h2><span className='arrow'>›</span></div><div className='hero'>{d.hero}</div>{d.lines.map(([a,b])=><Line key={a} a={a} b={b}/>)}</button>};
 const salesRows=m.wins.map(d=>[d.job?.name||'—',d.closedAt?.slice(0,10)||d.issueDate||'—',money(d.priceWithTax)]),pendingRows=m.pending.map(d=>[d.job?.name||'—',d.issueDate||'—',money(d.priceWithTax)]),lostRows=m.losses.map(d=>[d.job?.name||'—',d.closedAt?.slice(0,10)||d.issueDate||'—',money(d.priceWithTax)]),opsRows=m.current.map(x=>[x.job,x.stage,money(x.remaining||x.value),x.vendor]),peopleRows=m.people.map(x=>[x.name,x.active,x.assignments,money(x.value)]),arRows=m.ar.map(d=>[d.job?.name||'—',d.issueDate||'—',money(d.balance)]),apRows=m.ap.map(d=>[d.account?.name||'—',d.job?.name||'—',money(d.balance)]);
 return <><style>{css}</style><div className='app'>
  <div className='head'><div className='brand'><img src={logo} className='logo'/><div><h1>TenanTurn</h1><div className='sub'>Owner Dashboard</div></div></div><div className='small muted'>{data.fetchedAt?`Live JobTread · ${new Date(data.fetchedAt).toLocaleString()}`:'JobTread live feed'}</div></div>
  <div className='toolbar'><span className='small muted'>Performance period</span>{['Week','Month','Quarter','Custom'].map(x=><button className={mode===x?'on':''} onClick={()=>setMode(x)} key={x}>{x}</button>)}{mode==='Custom'&&<><input type='date' value={customStart} onChange={e=>setCustomStart(e.target.value)}/><input type='date' value={customEnd} onChange={e=>setCustomEnd(e.target.value)}/></>}<span className='periodlabel'>{period} · Ops is current as of now</span></div>
  {loading&&<div className='notice'>Loading live JobTread data…</div>}{error&&<div className='notice'>Live data is not connected in this preview yet: {error}</div>}
  <div className='ownergrid'><Card name='Sales'/><Card name='Ops'/><Card name='People'/><Card name='Finance'/></div>
  <section className='detail'>
   <div className='detailhead'><div><h2>{active}</h2><div className='small muted'>{active==='Ops'?'Current operating position':'Performance for '+period}</div></div><div className='small muted'>Click another box above to switch departments</div></div>
   {active==='Sales'&&<div className='grid'><Metric label='Sales Won' value={money(m.salesWon)} tone='good'/><Metric label='Pending Pipeline' value={money(sum(m.pending,d=>d.priceWithTax))}/><Metric label='Win Rate' value={pct(m.winRate)}/><Metric label='Lost Decisions' value={m.losses.length}/><div className='card c6'><h3>Won</h3>{salesRows.length?<Table headers={['Job','Approved','Value']} rows={salesRows}/>:<div className='empty'>No wins in this period.</div>}</div><div className='card c6'><h3>Pending Now</h3>{pendingRows.length?<Table headers={['Job','Sent','Value']} rows={pendingRows}/>:<div className='empty'>No pending bids.</div>}</div>{lostRows.length>0&&<div className='card c12'><h3>Lost Decisions</h3><Table headers={['Job','Decision','Value']} rows={lostRows}/></div>}</div>}
   {active==='Ops'&&<div className='grid'><Metric label='Backlog' value={m.backlog.length}/><Metric label='Assigned / Not Started' value={m.assigned.length}/><Metric label='WIP' value={m.wip.length} tone='blue'/><Metric label='Ready to Bill' value={m.ready.length} tone='good'/><div className='card c12'><h3>Current Work</h3>{opsRows.length?<Table headers={['Job','Stage','Remaining','Vendor']} rows={opsRows}/>:<div className='empty'>No current approved work found.</div>}</div>{m.review.length>0&&<div className='card c12'><h3>Needs Review</h3><div className='small muted'>These jobs have activity that does not reconcile cleanly to the normal stage rules.</div><Table headers={['Job','Stage','Remaining','Vendor']} rows={m.review.map(x=>[x.job,x.stage,money(x.remaining||x.value),x.vendor])}/></div>}</div>}
   {active==='People'&&<div className='grid'><Metric label='Active Vendors' value={m.people.filter(x=>x.active>0).length}/><Metric label='Active Assignments' value={sum(m.people,x=>x.active)}/><Metric label='Assigned Work' value={money(sum(m.people,x=>x.value))}/><Metric label='Vendor Records' value={m.people.length}/><div className='card c12'><h3>Vendor Load</h3>{peopleRows.length?<Table headers={['Vendor','Active Jobs','Assignments','Active Work']} rows={peopleRows}/>:<div className='empty'>No vendor work orders found.</div>}</div></div>}
   {active==='Finance'&&<div className='grid'><Metric label='Earned Revenue' value={money(m.revenue)}/><Metric label='Job Profit' value={money(m.profit)} tone='good'/><Metric label='Adjusted Margin' value={pct(m.margin)}/><Metric label='A/R Now' value={money(sum(m.ar,d=>d.balance))}/><div className='card c6'><h3>Period Profit</h3><Line a='Earned production revenue' b={money(m.revenue)}/><Line a='Production cost' b={money(m.cost)}/><Line a='Historical payment fees' b={money(m.fees)}/><Line a='Job profit' b={money(m.profit)} tone='good'/><Line a='Adjusted margin' b={pct(m.margin)}/><div className='foot'>Pass-through reimbursements excluded from revenue and cost. Draft invoices and draft bills excluded from A/R and A/P.</div></div><div className='card c6'><h3>Current Receivables / Payables</h3><Line a='A/R' b={money(sum(m.ar,d=>d.balance))}/><Line a='A/P' b={money(sum(m.ap,d=>d.balance))}/><Line a='Pass-throughs in period' b={money(m.reimbursements)}/></div><div className='card c6'><h3>A/R Detail</h3>{arRows.length?<Table headers={['Job','Invoice Date','Balance']} rows={arRows}/>:<div className='empty'>No outstanding issued invoices.</div>}</div><div className='card c6'><h3>A/P Detail</h3>{apRows.length?<Table headers={['Vendor','Job','Balance']} rows={apRows}/>:<div className='empty'>No outstanding approved vendor bills.</div>}</div></div>}
  </section>
 </div></>;
}
